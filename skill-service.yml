server:
  port: 8084

spring:
  application:
    name: skill-service
  datasource:
    url: jdbc:mysql://localhost:3306/skillsync_skill
    username: root
    password: Shally@183
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cache:
    type: simple

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
