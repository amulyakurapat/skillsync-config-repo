server:
  port: 8086

spring:
  application:
    name: review-service
  datasource:
    url: jdbc:mysql://localhost:3306/skillsync_review
    username: root
    password: Shally@183
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
